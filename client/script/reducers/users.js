const initState = {
  currentUser:{},
  friendLists:[],
  channelLists:[],
  error:null
}
export default function users(state = initState, action) {
  switch (action.type) {
    default:
      return state
  }
}
